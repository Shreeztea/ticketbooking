PORT=3001
# For macOS with Homebrew PostgreSQL, use your system username (no password needed for local)
# Replace 'shristishresthasemantic' with your actual PostgreSQL username if different
DATABASE_URL=postgresql://shristishresthasemantic@localhost:5432/ticketbooking
# Alternative: If you have a password set, use: postgresql://username:password@localhost:5432/ticketbooking
NODE_ENV=development
